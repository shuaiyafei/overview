
### codePointAt() 方法

在给定字符串中按位置提取Unicode代码

### String.fromCodePoint()方法

### normalize()方法

### 正则表达式U标志

### 字符串的其他改动

	trim()方法

### 识别子字符串的方法

- includes方法 给定文本存在于字符串的任意位置返回true
- startsWith方法 在给定文本出现在字符串起始处返回true 否则返回false
- endsWith方法 在给定文本出现在字符串结尾处返回true 否则返回false


### repeact()方法

接受一个参数作为字符串的重复次数 返回一个将初始字符串重复指定次数的新字符串

### 正则表达式的其他改动

### 正则表达式y标志

### 复制正则表达式

### flags属性

### 模板字面量

- 多行字符串 针对多行字符串的形式概念
- 基本的字符串格式化 将字符串部分替换为已存在的变量值的能力
- html转移 能转换字符串以便于安全插入到html中的能力

### 基本语法

### 制造替换位

### 标签化模板

